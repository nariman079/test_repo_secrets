name: Test secrets

on:
  pull_request: 
  push: 
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    environment: develop
    
    steps:
      - name: Simple run
        run: echo "Test simple run"

      - name: Get secrets 
        run: echo ${{ secrets.TELEGRAM_BOT_TOKEN }}
